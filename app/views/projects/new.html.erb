<h1>New Project</h1>
<%= form_for(@project) do |f| %>
<!---form_for is Rails way of building form for Active Record object the 'f' is the FormBuilder object->
    <p>
        <%= f.label :name %><br>
        <%= f.text_field :name %>
    </p>
    
    <p>
        <%= f.label :description %><br>
        <%= f.text_field :description %>
    </p>
    
    <%= f.submit %>
<% end %>